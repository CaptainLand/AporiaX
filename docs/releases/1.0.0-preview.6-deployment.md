# Preview 6 发布与部署记录

2026-09-25。用户批准 Cloud/Web 部署、桌面打包并发布 GitHub Release。

## 发布结果

- Release：https://github.com/CaptainLand/AporiaX/releases/tag/v1.0.0-preview.6 ，普通 Release，已设为 Latest（版本名仍为 preview）。
- 精确源码标签：`v1.0.0-preview.6`，提交 `1f8673645022bae06f2b748e49f315c5aba6a1d6`。
- 发布分支：`codex/cloud-quota-wind-down`。未强推 main，主工作区无关修改保留。
- 安装包目录：`D:/Agent开发/.tmp/cloud-parity-preview5/release`（目录沿用旧名，包内版本为 Preview 6）。
- 安装版 SHA256：`d0d487d9c1b4ddcd9bae632cda05e650461c9ef9a70ad6d9b8f4a2f3ad4bdacc`。
- 便携版 SHA256：`6241a5205a737204a035ee4b71a136462784d0923615cf9fab515caf964c7ad8`。
- 公网镜像：`https://101.43.44.160/downloads/`；网页下载链接及两个更新频道均为 Preview 6。

## 线上状态与边界

- Cloud API/Gateway/Worker：`aporiax-cloud-runtime:preview6-20260925`。
- 镜像 ID：`sha256:ddbf691ba340e9206ac64f4a5f5c28626bdea2ab27bd0a6bd26dc548b7dd4f23`。
- Web：`/var/www/aporiax-beta/releases/20260925-preview6`，头像入口默认关闭，不删除已有头像。
- 用户额度取消最大输出预占，按实际结算；耗尽后停止新派发。新桌面在确认正额度不超过 5% 时进入收尾。
- 保留每日 ¥5、20 人自助注册、全局/用户/设备并发 4/2/2、周额度、鉴权及幂等账务。
- 最低客户端仍为 Preview 4，最新为 Preview 6；新收尾和保存暂停体验须用 Preview 6。
- 代理安全配置 SHA256 仍为 `9e54e25b5c72a2ca2e503e5db48da4a24ba5f383edcf361b30823f4f93eaddd1`；未改 DNS、TLS、登录方式、密钥、账号或数据库结构。
- 手机远程接口继续关闭。模型请求仍需发送必要上下文，不重新开放电脑文件/项目/对话后台同步。

## 验证证据

- 本地 Cloud：额度准入 7 组，兼容单元 20/20，真实隔离 PostgreSQL + 模拟供应商 HTTP 集成 28/28；TypeScript 构建通过。
- 桌面：5% 收尾 15、额度暂停恢复 15、模型一致性 11、Cloud 兼容 17、外层恢复 12；子任务生命周期、账户菜单浏览器、隐私通道移除、更新运行时通过。
- 修复打包脚本会把旧 `preview.yml` 复制覆盖新 `latest.yml` 的问题；增加双方频道都可能是新版本、无匹配版本拒绝发布的测试。
- 安装包 225 个源码/资源与本地构建逐字节一致，22 个原生 PTY 文件正确解包；无私有根目录产物，SHA512/大小匹配。
- GitHub 六项资产大小和 SHA256 均与本地一致；镜像五项校验与更新清单通过。
- 真实 Preview 6 安装/便携模式在空白隔离资料目录启动：无更新入口隐藏、公网 PKCE URL 及版本正确，无页面异常。没有覆盖安装用户程序。
- 真实 Preview 4 安装模式启动后发现 Preview 6，并显示左下角更新入口；未自动执行安装。
- 公网浏览器：主页、登录弹窗、账户深链/刷新及教程正常；只请求同源 API，无页面异常，无真实账号凭据提交。
- 匿名账户/Gateway/Admin 返回 401，已移除同步路径 404。API/Gateway 健康，Worker 运行，旧代理配置哈希保持一致。
- 未调用真实付费模型，未消耗真实账号额度。这些模拟测试不等于供应商端到端测试或保证模型完全遵从收尾指令。

## 备份与回退

- 私有备份：`/srv/aporiax-cloud-preview/backups/preview6-20260925`，数据库 dump 权限 600，`pg_restore --list` 验证可解析。未下载用户数据库。
- 保留原镜像 `aporiax-cloud-runtime:preview5-20260925r1`、原 release-policy、原源代码、Web symlink 目标、Nginx 配置及旧频道文件。
- 如需回退，恢复该备份的应用策略及 Web/频道文件，使用 `deploy/friends-beta/compose.sh` 重建 API/Gateway/Worker，验证并重载 Nginx。不要恢复旧数据库覆盖新账单，不删除卷。撤回 GitHub Latest 需明确维护者决定。
- 激活脚本有失败回退；本次切换成功，无数据库迁移或账号清理。
- D 盘归档时空间耗尽：只删除本轮失败的 `D:/Agent开发/preview6-downloads.tar.gz`（可重新生成），改为直接上传安装包。未删除用户文件；本地磁盘仍仅约 250 MB 空闲，需另行清理。

## 源码

桌面：主工作区与 `.tmp/cloud-parity-preview5`；Cloud：`.tmp/aporiax-cloud-server-preview`；Web：`.tmp/aporiax-web-guide`。网页生产构建是 `.tmp/aporiax-web-guide/.tmp/preview6-public`，不是 private-preview 构建。

采用已确认规格作为上线验收标准，并按网页部署规范验证真实浏览器及安全门禁；没有额外改版。
