# 1.0.0-rc.1 本地候选验收

日期：2026-09-26。范围：本地源码、Windows x64 安装版及便携版；不发布 GitHub、不改 Cloud/Web、不更改账户与版本限制。

## 本轮验证状态

- [x] package 与 lockfile 版本一致：1.0.0-rc.1。
- [x] Preview → RC → Stable 版本排序及拒绝降级测试通过。
- [x] RC 渠道元数据与旧渠道文件混用保护测试通过。
- [x] 当前 RC 源码完整审计：81/81 脚本通过。
- [x] 当前 RC 浏览器及 Electron 桌面回归：17/17 项通过；另补跑引用、扩展、更新和澄清提问 4 个浏览器脚本，全部通过。
- [x] Vite 生产构建通过。既存主 JS chunk 为 1,560.65 kB（gzip 475.90 kB），仍有大 chunk 提示；没有将此提示隐藏。
- [x] 安装包、便携包、230 个源码/资源逐字节及 SHA-512/大小核验通过；22 个 PTY 原生文件正确解包，未包含检查清单中的私人根文件或已移除手机通道。
- [x] 隔离配置下实际包启动、运行版本、更新入口与登录 URL 验证通过（安装模式和便携模式各一次）。

## 产物

输出目录：`release/v1.0.0-rc.1/`。

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| AporiaX-Setup-1.0.0-rc.1-x64.exe | 148449289 | `47abf7e76e6d997991f675591e722386ba8d186ccff6b88166d489cdc7894114` |
| AporiaX-Portable-1.0.0-rc.1-x64.exe | 148150081 | `a5cf60915eedfd02e9e274cd7b2ef91d96419f13acdbaa935ad78d617e4a3e9c` |

同目录含 `SHA256SUMS-1.0.0-rc.1.txt`、安装器 blockmap、`rc.yml` 和 `latest.yml`。后两者均为此次 RC 的本地元数据，没有上传或改变公网 Latest。

PowerShell `Get-AuthenticodeSignature` 对 Setup 和 Portable 均返回 `NotSigned`。构建器打印 signing 步骤不代表存在可信代码签名。

## 本次证据与测试边界

- 环境：Windows，Node v22.19.0；本地 Git 基线 `e997cc9902f85189b83d3a1ab127f57e1cfa1d73`，加当前未提交修改，不冒充干净发布 tag。
- 完整审计：`.tmp/audit-results/rc1-20260926.json`。
- 桌面门禁：`.tmp/audit-results/desktop.json`（本轮运行时 17/17）。含真实 PowerShell PTY、生产 file:// 文档 iframe/CSP，其他浏览器场景使用本地测试夹具。
- 额外浏览器脚本：`mention-regression-browser.mjs`、`context-extensions-browser.mjs`、`app-update-browser.mjs`、`user-clarification-browser.mjs`；覆盖慢候选源隔离、扩展错误/恢复、无更新隐藏、任务运行时安装保护、定位并回答澄清问题；无新增页面异常。
- 成品测试命令：`node tests/preview5-packaged.mjs release/v1.0.0-rc.1`。实际启动 `win-unpacked/AporiaX.exe`，分别使用安装模式和便携模式环境；不是完整安装向导/便携外壳解压验收。
- 两次启动使用新建空白 APPDATA/LOCALAPPDATA/user-data-dir，不读取真实用户配置；运行时版本 `1.0.0-rc.1`，`app.isPackaged=true`。启动检查结果为 `not-available`，侧栏更新入口未显示。
- 点击登录后捕获系统浏览器链接而不实际完成登录：源站为现有 `https://101.43.44.160`，版本参数正确、PKCE S256 存在。未发送真实验证码、未创建 Cloud 用户、未调用付费模型。
- 截图：`.tmp/packaged-1.0.0-rc.1/profile-25gP4V/app.png`、`.tmp/packaged-1.0.0-rc.1/profile-gtiaeh/app.png`。两个测试应用均正常关闭。
- Docker 引擎未启动，不能把相关配置检查说成 Docker 真机运行验证。
- 本轮按 web-development 的构建与实际浏览器验证要求执行；没有跨过部署门槛。

## 正式版前仍需验收

- 干净 Windows 环境、外部电脑、安装向导及旧版数据升级/失败回退。
- 实际邮件登录、真实模型任务、多 Agent 长任务、物理睡眠/断网和持续使用观察。
- Cloud 独立的备份恢复、告警、防滥用、正式域名和运维验收。
- 最终许可条款、可信签名方案、固定 tag 的远端 CI 和发布审批。

本地工作树包含此前已授权修改，未创建发布 tag 或声称来自干净发布提交。包内源文件与本地输入需逐字节核对；公开发布前仍要整理固定提交、重跑远端门禁。
