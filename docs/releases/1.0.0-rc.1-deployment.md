# RC1 公开发布与 Web / Cloud 部署记录

2026-09-26。用户授权日额度百分比展示、Web / Cloud 部署和 GitHub RC1 默认更新发布。

## 结果

- GitHub Release：https://github.com/CaptainLand/AporiaX/releases/tag/v1.0.0-rc.1 。公开、非 GitHub prerelease、Latest，以便默认更新器发现；产品版本仍为 RC，不是正式 1.0.0。
- 精确标签源码：`v1.0.0-rc.1` → `7b90810e8a8ff2448b4a3c7f31e937e119ebc837`，分支 `codex/release-1.0.0-rc.1`。未强推 main，未覆盖历史 Release。
- **此次公开包**：主工作区 `.tmp/cloud-parity-preview5/release/v1.0.0-rc.1/`。根目录 `release/v1.0.0-rc.1/` 是先前未发布的初始候选包，不是此次公开文件。更新后的发行说明随新包重新构建。
- 安装版：148447866 bytes，SHA256 `ff67783ca7a6b06aeae79e9fea7ddb6a94d7d6b29d87961a6e791b690578a372`。
- 便携版：148148654 bytes，SHA256 `8729037d3f242958b6720f39f9008cea1767934f785d0094ffdb1fd23b4a3b76`。
- 安装包仍未可信代码签名。便携版手动换包；没有覆盖安装用户当前软件。
- 网站：https://101.43.44.160 。Web current：`/var/www/aporiax-beta/releases/20260926-rc1`。
- Cloud：`aporiax-cloud-runtime:rc1-20260926`，镜像 ID `sha256:1c42e0c82e94ae3a84442358d2cb6c6b3c06d977b14ed2be59cd0ae0b73ffd2d`。
- HTTPS 下载镜像与 `latest.yml`、`preview.yml`、`rc.yml` 均指向 RC1；网站 Windows 下载已更新。

## 本次改动与保留边界

- 全站日额度继续复用 `QuotaBar` / `GemLayer`，移除覆盖其动态效果的样式。剩余、已用、处理中和待核对只显示百分比；正数不足半个百分点显示 `<1%`，未知/失败/过期显示 `—`。
- 中英文注册及产品介绍去掉固定货币金额，保留共享日额度及耗尽后的行为说明。字号和布局不变，头像和手机同步仍未开放。
- 受保护的网页 `/api/quota/daily-shared` 输出白名单 `provider-daily-ratio-v1`，无金额、币种、其他用户或请求身份；保留鉴权、no-store 和同源限制。
- **这不是对所有协议做金额保密**：旧桌面依赖的 Gateway 用量回执仍保持原金额契约，以兼容收尾判断；后台账务仍使用精确金额。不得宣称 API 全面隐藏运营成本。
- 实际日上限、周额度、20 人自助注册、全局/用户/设备并发 4/2/2 未变。
- Cloud 最低版本仍 `1.0.0-preview.4`，最新版本改为 `1.0.0-rc.1`。没有额外拒绝旧版登录。
- 无数据库结构迁移、用户数据清理、DNS/TLS、防火墙或授权政策调整。Nginx 最终 SHA256 保持 `5f2748bc3e89185b11b112923d14be9b3173c6d71b289efaab5797ab782dd826`。

## 本次重新验证

- 精确发布源码：81/81 审计、17/17 桌面门禁（含真实 PowerShell PTY 与生产 Electron 文档隔离）；源码标签、干净跟踪文件验证通过。
- 新包 230 个源码/资源逐字节匹配，22 个原生 PTY 文件解包，私人根目录产物/已移除同步代码未混入，更新 SHA512 与文件大小正确。
- 实际新包的安装/便携模式空白资料启动通过：启动检查更新、无更新隐藏、生产 PKCE URL 和 RC1 app_version，无页面错误。测试并非完整安装向导或便携外壳解压测试。
- Cloud 21/21 单元、29/29 隔离 PostgreSQL + 假供应商集成：百分比投影、鉴权、幂等、并发、结算和未知账务回归通过。仅本机空白库；测试集群已停止。
- Web 修改前基线、修改后开发/生产构建浏览器回归通过：中文/英文、100%、低额度、耗尽、活动/未知占用、失败/过期、手动/30 秒/切回刷新、390px 与桌面布局、动态效果与周额度一致。生产首页检查覆盖 320–1440px、教程/登录和对比度。
- GitHub 六项资产 SHA256/大小与本地匹配；服务器正式下载目录全部校验通过。公开入口 HTML / 主 JS / CSS 与本地生产构建一致。
- 上线后 HTTPS 浏览器主页/新下载链接/匿名登录弹窗/账户深链与刷新/教程无页面异常；API 同源，日额度匿名 401、外站 Origin 403、no-store；Admin 401、退役同步接口 404。
- 线上只读汇总检查通过，无用户明细输出、无新测试账号或真实模型请求。
- **真实旧 Preview 4 安装模式启动检测到 RC1，并显示左下角更新入口**。使用隔离空白资料；未读取或覆盖用户资料，未自动安装。
- 未声称真实付费模型长任务、旧用户资料覆盖安装、干净机器及长期稳定性已验收。

## 回退与归档

- 备份 `/srv/aporiax-cloud-preview/backups/rc1-20260926`：原发布策略、源码、Nginx、Web symlink、latest/preview 清单，以及已验证可解析且权限 600 的数据库 dump；数据库未下载。
- 切换时短暂停止接收新生成，等待活动模型租约归零，再停服务、备份和切换。应用部署脚本保留失败回退；此次未触发。
- 回退应用应恢复旧镜像/策略、Web symlink 和频道文件，**不要恢复旧数据库覆盖后续账务，不删除卷**。撤回 GitHub Latest 应单独决定。
- Cloud 候选代码归档 SHA256：`fa62298043cf12b8699c4e9a7750c9f6ab2565405d0f9d0ebcc735a1f0fa3a18`。
- Web 静态归档 SHA256：`71b6fd7f61f8cc7b07eedf5c6e76ed75417ee027caf3d74eee1c2f2416b348a9`。
- 两份候选归档和版本化发布脚本已在服务器持久保存，不含 `.env`、密钥、用户数据库或 node_modules。
- 开发源仍为根目录 Desktop、`.tmp/aporiax-web-guide`、`.tmp/aporiax-cloud-server-preview`，保留其已有改动。Web/Cloud 通过归档部署，不整树推送其历史未提交内容。
- 按 ui-design/web-development 技能保留原设计、检查失败/窄屏状态并执行可回退发布门禁。未提供 agent-browser 工具，采用项目已有 Playwright + Edge 验证。
