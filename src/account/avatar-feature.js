// Temporarily retired from the UI. Keep the implementation for a future review.
// Build-time opt-in only; account data or browser storage cannot enable it.
export const PROFILE_AVATARS_ENABLED = import.meta.env.VITE_APORIAX_PROFILE_AVATARS === "true";
