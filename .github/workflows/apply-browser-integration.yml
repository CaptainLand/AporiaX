name: Apply browser integration patch

on:
  push:
    branches:
      - agent/browser-v1

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[browser-patch]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/browser-v1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply exact browser integration patch
        run: node scripts/apply-browser-integration.mjs
      - name: Commit generated integration
        run: |
          git config user.name "AporiaX Automation"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "feat: wire browser tools into agent runtime [browser-patch]"
          git push origin HEAD:agent/browser-v1
